interface:
  display_name: "DaVinci Resolve MCP compatibility"
  short_description: "Use the optional Resolve MCP transports directly"
  default_prompt: "Use $resolve-mcp to operate Resolve through its MCP compatibility transport."
dependencies:
  tools:
    - type: "mcp"
      value: "davinci-resolve"
      description: "Local DaVinci Resolve MCP server for live Studio control"
policy:
  allow_implicit_invocation: false
